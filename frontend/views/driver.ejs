<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Driver Dashboard</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white p-8">
  <p>hello driver</p>
  <!-- <h1 class="text-3xl mb-4">Hello <%= user.username %>, your route:</h1>
  <p><strong>Location:</strong> <%= user.location %></p>
  <p><strong>Destination:</strong> <%= user.destination %></p>
  
  <h2 class="text-2xl mt-6">Clients looking for rides:</h2>
  <% if (clients.length > 0) { %>
  <ul>
    <% clients.forEach(client => { %>
    <li><%= client.username %> — <%= client.location %> to <%= client.destination %></li>
    <% }) %>
  </ul>
  <% } else { %>
  <p>No clients found matching your route.</p>
  <% } %> -->
  
<!--   
  <form action="localhost:8080/submit" , method="post">
     <h1 class="text-3xl mb-4">Hello <%= user.username %>, your route:</h1> -->

</form>

</body>
</html>
